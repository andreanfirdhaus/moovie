const { info } = require("autoprefixer")
const { Warning } = require("postcss")

module.exports = {
    darkMode: "class",
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
};
